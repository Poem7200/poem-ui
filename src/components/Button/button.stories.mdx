import { Meta, Canvas, Story } from '@storybook/addon-docs'
import Button from "./button";

<Meta title="testing" component={Button}  />

export const Template = (args) => (
  <Button {...args}></Button>
)

# this is testing

<Canvas>
  <Story name='basic button' args={{
    children: 'default button'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
